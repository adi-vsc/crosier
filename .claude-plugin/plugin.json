{
  "name": "crosier",
  "version": "0.2.0",
  "description": "Zero-context second opinions for long Claude Code sessions. /crosier:attack sends the latest decision to a subagent that has never seen the session. An experimental Stop gate, off by default, reviews risky final answers before the turn ends.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/crosier_hook.py\" || python \"${CLAUDE_PLUGIN_ROOT}/hooks/crosier_hook.py\" || py -3 \"${CLAUDE_PLUGIN_ROOT}/hooks/crosier_hook.py\"",
            "timeout": 90
          }
        ]
      }
    ]
  }
}
