{
  "name": "crosier",
  "version": "0.2.0",
  "description": "Install-and-forget second opinion for long Claude Code sessions. When a final answer claims an outcome or follows file edits, a fresh, zero-context Claude instance reviews it at Stop and holds the turn if it finds a problem, before the answer stands.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/crosier_hook.py\" || python \"${CLAUDE_PLUGIN_ROOT}/hooks/crosier_hook.py\" || py -3 \"${CLAUDE_PLUGIN_ROOT}/hooks/crosier_hook.py\"",
            "timeout": 90
          }
        ]
      }
    ]
  }
}
